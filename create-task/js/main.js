import { DOMSelectors } from "./dom";
import "../css/style.css";
import { cards } from "./cards"
//remake blackjack into a simpler card game(simple war card game) where when they press the button draw it will give the player and the dealer a card and whoever gets the bigger card number wins, if they get the same cards it will make them draw again